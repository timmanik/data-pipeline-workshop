version: '3'
services:
  flask_app:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - data-pipeline_mynetwork

networks:
  data-pipeline_mynetwork:
    external: true